{
  "name": "@cart-wise/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply cart-wise --local --persist-to ../../.wrangler/state"
  },
  "dependencies": {
    "@cart-wise/db": "workspace:*",
    "@cart-wise/shared": "workspace:*",
    "better-auth": "^1.7.5",
    "drizzle-orm": "^0.45.3",
    "hono": "^4.13.8",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "wrangler": "^4.137.0"
  }
}
